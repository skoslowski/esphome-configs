wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: ${name}
    password: !secret wifi_ap_password
  # use_address: 192.168.11.220

captive_portal:

api:
  password: !secret api_password

ota:
  password: !secret ota_password
